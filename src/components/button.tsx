import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

const Button = () => {
  return (
    <View>
      <Text> Button Component ddfddf</Text>
    </View>
  );
};
export default Button;
const styles = StyleSheet.create({});
